### Glossary

`raw` - utf8 that gets stored (store on disk)
`secretized` - raw parsed at object but with still encrypted values (for managing)
`exposed` - secretized but with decrypted values (for runtime)